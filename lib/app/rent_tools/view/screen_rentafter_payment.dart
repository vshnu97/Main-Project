import 'package:flutter/material.dart';

class ScreenRentAfterPayment extends StatelessWidget {
  const ScreenRentAfterPayment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
    
  }
}